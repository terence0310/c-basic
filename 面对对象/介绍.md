属性：继承，多态，封装。

# 访问权限
1. ==public  protect  private==
   %% class people{
public : int a;
protect:  int b;
private :  int c;
}; 就不能在外部赋值即访问，除public外%%

## 如何访问私有成员
class hero{
    public:
        void setH_name(string name){
           H_name=name;
        }
        string getH_name(){
            return H_name;
        }
        int getH_skill(){
            return H_skill;
        }
        void setH_speed(int speed){
            H_speed=speed;

        }

    private:
        string H_name;//read,write
        int H_skill;//read
        int H_speed;//write

};
在main中就可以访问

## 构造，析构，拷贝
1. 构造class hero{

    public:
        hero(){
            H_name = " ";
            H_skill = 0;
            H_speed = 0;
        }//无参构造

        hero(string name){
            H_name = name;
        }//有参构造函数

    private:
        string H_name;
        int H_skill;
        int H_speed;

};**==注意不需要写返回值==**
hero h2{} ==括号填写参数==
==hero h1 = hero（“terence”）;可以==


2. 析构class hero{
    public:
        hero(){
            cout<<"构造函数完毕"<<endl;
        }
        ~hero(){
            cout<<"析构函数完毕"<<endl;
        }
    private:
        string H_name;
        int H_skill;
        int H_speed;
};对于析构只有在它有返回值时，==它才析构完毕(例如函数中有hero h)==


3. 拷贝
- class hero{
    public:
        hero(){
            H_name="terence";
            H_skill=100;
            H_speed=100;
            cout<<"构造函数完毕"<<endl;
        }
        ==hero(const hero &h){==
            H_name=h.H_name;
            H_skill=h.H_skill;
            H_speed=h.H_speed;
            cout<<"拷贝构造函数完毕"<<endl;
        }
    private:
        string H_name;
        int H_skill;
        int H_speed；
};注意只要传入副本，就执行构造，只有定作用义域结束才拷贝完成。
- 用已经的对象来初始一个对象
- 传参

-----------------------------------------------------------------
# 初始化list
- hero(传参1，传参2)  :  m_hp(传参1)，m_name(传参2){}

-----
#静态变量static
class hero{
    public:
        hero(){
            H_name="terence";
            H_skill=100;
            H_speed=100;
            cout<<"构造函数完毕"<<endl;
        }
        static int hero_num;
    private:
        string H_name;
        int H_skill;
        int H_speed;
};

int hero::hero_num = 0;

int main(){
    hero h1;

   cout<<"英雄数量："<<hero::hero_num<<endl;
    h1.hero_num = 1;
    cout<<"英雄数量："<<h1.hero_num<<endl;
    ==cout<<"英雄数量："<<&(hero::hero_num)<<endl;==
    ==cout<<"英雄数量："<<&(h1.hero_num)<<endl;==
    return 0;
}**这里类与对象中hero_num地址一样**

#thins指针
- 主要防止变量冲突
  class hero{
    public:
        hero(int hp){
            this->hp = hp;
            cout<< hp <<"  "<< this->hp << endl;
        }
    private:
        int hp;
};this也是代表指针(对象的地址)，可以打印出来。
  

#const和mutable的使用
class Hero{
    public：
        Hero(int hp):m_hp(hp){}
        int getHp() const{
            m_speed++;
            return m_hp;
        }
    private:
        int m_hp;
        mutable int m_speed;
};const主要是防止改变值，而mutable是来改变const中某个变量的值























